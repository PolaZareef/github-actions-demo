name: Hello World

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions!"

      - name: Print date
        run: date

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls -la